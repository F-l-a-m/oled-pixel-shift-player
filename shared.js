// Shared message names for the popup, service worker and injected script.
globalThis.OLED_ACTIONS = Object.freeze({
    REQUEST: "oled-request",
    START: "start",
    STOP: "stop",
    TOGGLE: "toggle"
});
